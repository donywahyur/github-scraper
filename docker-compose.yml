version: '3.8'

services:
  scrapper:
    build: .
    env_file:
      - ./.env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # Optional: Add nginx reverse proxy
  # nginx:
  #   image: nginx:alpine
  #   container_name: github-scraper-nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - api
  #   restart: unless-stopped
